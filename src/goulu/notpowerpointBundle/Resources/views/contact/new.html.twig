<!DOCTYPE html>
<html>
    <head>
        <title>This is not powerpoint contact site</title>
    </head>
    <body>
        <div id="header">
            <h1>Leave your information here</h1>
        </div>
        <div id="contactForm">
            <form action="{{ path('task_new') }}" method="post" {{ form_enctype(form) }}>
                {{ form_widget(form) }}
                <input type="submit" />
            </form>
        </div>
    </body>
</html>
